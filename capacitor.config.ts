import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.utility.chessassistant',
  appName: 'Chess Assistant',
  webDir: 'dist'
};

export default config;
